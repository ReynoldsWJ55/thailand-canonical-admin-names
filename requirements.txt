# Build- and test-time dependencies for the Thailand Canonical Administrative-Names Reference.
# Pinned to the versions used to produce v1.0.2 outputs (smoke-tested 2026-05-06).
# v1.0.2 switched the canonical Parquet engine from fastparquet to pyarrow; see
# CHANGELOG. fastparquet is retained as an optional install for environments
# that prefer the smaller wheel, but the build scripts call engine="pyarrow".

# Core build pipeline
pandas==2.3.3
shapely==2.1.2
pyproj==3.7.1
numpy==2.2.6

# Parquet I/O (canonical engine as of v1.0.2)
pyarrow==24.0.0

# Methodology PDF compile (system pandoc + xelatex still required separately)
# No Python packages; pandoc is a binary dependency.
